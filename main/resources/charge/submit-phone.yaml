post:
  tags:
    - Charge
  summary: Submit Phone
  operationId: charge_submitPhone
  requestBody:
    content:
      application/x-www-form-urlencoded:
        schema:
          $ref: '../../schemas/charge/SubmitPhone.yaml'
      application/json:
        schema:
          $ref: '../../schemas/charge/SubmitPhone.yaml'
  responses:
    200:
      $ref: '../../responses/ok.yaml'
    401:
      $ref: '../../responses/unauthorized.yaml'
    default:
      description: Server error