description: Request successful
content:
  application/json:
    schema:
      $ref: '../response.yaml'
    examples:
      Initiailize Transaction:
        $ref: '../../examples/transaction/initialize.yaml'
