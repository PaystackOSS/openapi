post:
  tags:
    - Subscription
  summary: Enable Subscription
  operationId: subscription_enable
  requestBody:
    content:
      application/x-www-form-urlencoded:
        schema:
          $ref: '../../schemas/subscription/Toggle.yaml'
      application/json:
        schema:
          $ref: '../../schemas/subscription/Toggle.yaml'
  responses:
    201:
      $ref: '../../responses/created.yaml'
    401:
      $ref: '../../responses/unauthorized.yaml'
    default:
      description: Server error