type: object
properties:
  status:
    type: boolean
  message:
    type: string
  data:
    type: array
    items:
      $ref: ./BulkChargeListResponseArray.yaml
  meta:
    type: object
    properties:
      total:
        type: integer
      skipped:
        type: integer
      perPage:
        anyOf:
          - type: integer
          - type: string
      page:
        type: integer
      pageCount:
        type: integer
    required:
      - total
      - skipped
      - perPage
      - page
      - pageCount
    additionalProperties: false
required:
  - status
  - message
  - data
  - meta
