type: object
properties:
  status:
    type: boolean
  message:
    type: string
  data:
    type: object
    properties:
      name:
        type: string
      description:
        type: string
      currency:
        type: string
      price:
        type: integer
      quantity:
        type: integer
      is_shippable:
        type: boolean
      unlimited:
        type: boolean
      files:
        type: array
        items: {}
      shipping_fields:
        type: object
        properties:
          delivery_note:
            type: string
          shipping_address:
            type: string
          shipping_fees:
            type: array
            items: {}
        required:
          - delivery_note
          - shipping_address
          - shipping_fees
      integration:
        type: integer
      domain:
        type: string
      metadata:
        type: object
        properties:
          background_color:
            type: string
        required:
          - background_color
      slug:
        type: string
      product_code:
        type: string
      quantity_sold:
        type: integer
      type:
        type: string
      active:
        type: boolean
      in_stock:
        type: boolean
      minimum_orderable:
        type: integer
      maximum_orderable:
        nullable: true
      low_stock_alert:
        type: boolean
      id:
        type: integer
      createdAt:
        type: string
      updatedAt:
        type: string
    required:
      - name
      - description
      - currency
      - price
      - quantity
      - is_shippable
      - unlimited
      - files
      - shipping_fields
      - integration
      - domain
      - metadata
      - slug
      - product_code
      - quantity_sold
      - type
      - active
      - in_stock
      - minimum_orderable
      - maximum_orderable
      - low_stock_alert
      - id
      - createdAt
      - updatedAt
required:
  - status
  - message
  - data
