tags:
  - Plan
summary: Create Plan
operationId: plan_create
requestBody:
  content:
    application/x-www-form-urlencoded:
      schema:
        $ref: '../../schemas/plan/Create.yaml'
    application/json:
      schema:
        $ref: '../../schemas/plan/Create.yaml'
responses:
  201:
    $ref: '../../responses/plan/create.yaml'
  401:
    $ref: '../../responses/unauthorized.yaml'
  default:
    description: Server error