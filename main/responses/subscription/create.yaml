description: Create Subscription
content:
  application/json:
    schema:
      $ref: '../response.yaml'
    examples:
      Create Subscription:
        $ref: '../../examples/subscription/create.yaml'