description: Request successful
content:
  application/json:
    schema:
      $ref: './response.yaml'
    examples:
      Resolve Account Number:
        $ref: '../examples/identity-verification/resolve-account-number.yaml'
      Create Subaccount:
        $ref: '../examples/subaccount/create.yaml'
      Create Split:
        $ref: '../examples/split/create.yaml'

                  