description: Resource created
content:
  application/json:
    schema:
      $ref: './response.yaml'
    examples:
      Create Transfer Recipient:
        $ref: '../examples/transfer-recipient/create.yaml'